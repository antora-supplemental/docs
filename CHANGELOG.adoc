= Changelog
:toc:

== 2026-07-31 — antora-supplemental.org domain map

* `site.url` → `https://docs.antora-supplemental.org`; `site_home_url` → apex `https://antora-supplemental.org/`.
* Deploy writes `CNAME` (`docs.antora-supplemental.org`) into the Pages artifact.
* Registry / themes discovery points at `registry.antora-supplemental.org` (standalone themes gallery archived).

== 2026-07-31 — Rolling Valentus v2 pin

* Subscribe the hub UI bundle to the rolling major line `releases/download/v2/ui-bundle.zip` (with `snapshot: true`) instead of an exact patch tag, so sticky-footer and other 2.x fixes land on the next deploy without a playbook bump.

== 2026-07-31 — Valentus 2.0.2 sticky footer

* Pin UI bundle to Valentus `v2.0.2` (`2.0.2+antora.3`) so the hub picks up the sticky footer (shell fills leftover viewport; footer stays natural height / full-bleed).
* Refresh `pnpm-lock.yaml` for `@antora-supplemental/incremental` and `@antora-supplemental/serve`; clone `antora-serve` on CI so the `file:` serve dep resolves.

== 2026-07-30 — Org config & theme product roles

* Add `theme-products.adoc` — product map for `valentus-theme`, `antora-dark-mode`, and retired `antora-dark-theme`.
* Add `org-github-config.adoc` — `.github` vs `.github-private`, retired personal private bootstrap, `antora-dark-theme` successor pointers, org rename history (folds `ORG_RENAME.adoc`).
* Portal: dark-mode page documents successor split (valentus / antora-dark-mode) and obsolete personal forks.
* Cross-link private workflow templates from `antora-github-actions.adoc`; expand component list on the portal index.

== 2026-07-25 — Content-source dispatch rebuild

* Deploy workflow listens for `repository_dispatch` types `docs-source-updated` and `extension-updated` (push-triggered full site rebuild, no polling).
* Document agent-friendly notify via `gh api .../dispatches` in `antora-deployment.adoc`.
* Add `site-rebuild.adoc` — canonical v1/v2 architecture page for short PR/discussion links.

== 2026-07-25 — Valentus 2.0.0

* Pin UI bundle to Valentus `v2.0.0` (`2.0.0+antora.3`) release `ui-bundle.zip` (was broken `antora-dark-mode` pin / duplicate YAML keys on `main`).
* Refit org brand overlay for Valentus 2.x: token overrides (`--adt-canvas` / `--adt-accent` / …) instead of element scrapes that fight `site-visual.css`.
* Update `head-meta.hbs` for 2.x contract — read-width FOUC, Plus Jakarta Sans + IBM Plex Mono fonts, load `site-visual.css` + `site-read-width.css`, then `site-org-brand.css`.
* Expand `local-antora-playbook.yml` to a full playbook (Antora has no YAML `extends`); keep duplicate-key fix from the prior Valentus prep.


== 2026-07-24 — Valentus + org brand

* Switch UI bundle from `antora-dark-mode` to link:https://github.com/antora-supplemental/valentus-theme[valentus-theme] (1.x line).
* Green brand overlay retargeted at Valentus CSS variables (`site-org-brand.css`); header mark `img/logo.svg`.
* Preserve Valentus FOUC / dark-mode head script in `supplemental-ui/partials/head-meta.hbs`.
* Fix duplicate `branches` / `start_path` keys in playbooks.
